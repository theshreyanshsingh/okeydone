import Background from "@/components/Background";
import Footer from "@/components/Footer";
import Navigation from "@/components/Navigation";
import ScrollContainer from "@/components/ScrollContainer";
import Head from "next/head";

import * as erroranim from "../assets/animaticons/404.json";

const loaderOptions = {
  loop: true,
  autoplay: true,
  animationData: erroranim,
  rendererSettings: {
    preserveAspectRatio: "xMidYMid slice",
  },
};

import { LoaderScreen } from "@/utilities";
import dynamic from "next/dynamic";

const Lottie = dynamic(() => import("react-lottie"), { ssr: false });

export default function ErrorPage() {
  return (
    <>
      <Head>
        <title>Okay Done | Solutions</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <LoaderScreen />
      <main className={"main"}>
        <Background />
        <Navigation />
        <ScrollContainer>
          <div className="container">
            <div className="solutions-heading d-only">
              <Lottie
                options={loaderOptions}
                height={800}
                isClickToPauseDisabled={true}
                width={700}
              />
            </div>
            <div className="solutions-heading error m-only">
              <Lottie
                options={loaderOptions}
                height={300}
                isClickToPauseDisabled={true}
                width={300}
              />
            </div>
          </div>
          <Footer />
        </ScrollContainer>
      </main>
    </>
  );
}
